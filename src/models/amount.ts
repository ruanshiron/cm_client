export interface Amount2 {
  workshopId: string;
  workshopName: string;
  productId: string;
  productName: string;
  productCode: string;
  processId: string;
  processName: string;
  from?: string;
  to?: string;
  value: number;
}
